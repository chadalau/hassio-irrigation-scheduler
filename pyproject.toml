[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
# Start CI with correctness-oriented rules. Import/style cleanup can be
# introduced separately without rewriting the project's existing files.
select = ["E9", "F63", "F7", "F82"]
