"""Integration tests for the irrigation_scheduler Home Assistant integration.

These tests require pytest-homeassistant-custom-component and a real Home
Assistant installation; they are only collected when running under the HA test
venv (``pytest tests/integration`` or ``pytest tests`` there). The pure tests
in ``tests/`` are unaffected.
"""
